/**
 * CHEMIN SVG — ARCHÉ
 * Empty container - only words remain, positioned at line level
 */

export function CheminSVG() {
  return (
    <div
      style={{
        width: '100%',
        maxWidth: '100%',
        height: '90px' // Maintain vertical space where lines were (at y=90)
      }}
    />
  );
}
